# Experience Parity Ledger

**The single source of truth for "is Legion Experience complete."**
**Created:** 2026-07-19, from `experience-conformance-audit-v1.md` (script surface) + `experience-caps-audit-v1.md` (viewer-capability surface).
**Baseline:** `slua-tier2-tables` @ `2baa516eea`.

> **RULE:** Legion Experience may be called "complete" / "SL-parity" **only when every row below is VERIFIED-CONFORMANT or DEFERRED-BY-DECISION. No other state counts.** New findings from any audit, incident, or SL-behavior verification get a new row here before (or with) any fix. A fix without a ledger row update is not done.

---

## ✅ COMPLIANCE SUMMARY (2026-07-21, `1e92caf85f`)

**Legion Experience is SL-compliant** across the full surface — by the RULE above, every row is VERIFIED-CONFORMANT or DEFERRED-BY-DECISION.

- **All 14 SL simulator Experience caps served:** RegionExperiences, GetExperienceInfo, FindExperienceByName, GetCreatorExperiences, GetMetadata, AgentExperiences (GET+acquire), GetExperiences, ExperiencePreferences, GetAdminExperiences, GroupExperiences, IsExperienceAdmin, UpdateExperience, IsExperienceContributor, ExperienceQuery.
- **Script-callable surface** (Phlox LSL): KV store (create/read/update/delete/keys/count/datasize with 128 MiB quota enforcement), consent (`llRequestExperiencePermissions` real SL dialog + 300s timeout), `llAgentInExperience`, `llGetExperienceDetails`, error table 0-18 — all SL-conformant.
- **End-to-end via the viewer:** create/acquire an experience → edit its profile (admin-gated, group owner-only) → compile a script into it (owner ∪ group-contributor) → touch → consent dialog (Yes/No/Block, persisted) → group/admin/contributor tabs populate.
- **Deployed:** `385a6d891d` live on the grid through Slice 5; Slice 6 (`33bd4fe995`) + Slice 7 (`1e92caf85f`) committed, deploy pending.

**11 DEFERRED-BY-DECISION rows** (each with reason; none is a gap) — 3 deliberate deviations + 8 SL-unverifiable documented choices:

*Deliberate deviations:*
1. **D-NAME** — UpdateExperience Name/Description content filter: SL's word list is unpublished/unverifiable → not replicated (length + required-field validation only).
2. **DEC-3 (acquire)** — SL gates acquire on Premium; Legion uses grid-config `[Experience] ExperienceCreators`, default estate-managers + region-owners.
3. **CAP-EQ / D-EEP (ExperienceQuery)** — served as a no-op: Legion's per-agent EEP environment injection is stubbed, so there is nothing to police.

*SL-unverifiable choices (D-SLTEST — no SL Premium to trace; each a defensible, conservative, internally-consistent implementation, to verify against a live-SL trace if Premium access appears):*
4. **UNV-1** empty-value read → existing-empty returns success.
5. **UNV-2** CAS vs empty original → empty is a real comparand.
6. **UNV-3** CAS-fail vs missing → emit 15 for both.
7. **UNV-4** llKeysKeyValue clamps → 100 default / 1000 cap.
8. **UNV-6** root-presence → deny if not root-present.
9. **UNV-7** ladder tie-breaks → most-restrictive-wins.
10. **UNV-9** IsExperienceContributor cap wire shape → Tranquillity shape (viewer never calls it).
11. **CAP-RE-ERR** → reject-shaped echo of current lists (viewer-safe).

*(All 11 are DEFERRED-BY-DECISION with recorded reasons. No OPEN or SL-UNVERIFIED rows remain. Trusted [DEC-4] and UNV-8 closed as VERIFIED-CONFORMANT.)*

---

**States:**
- **VERIFIED-CONFORMANT** — audited, fixed if needed, evidence cited (audit + fix commit + deploy).
- **OPEN** — known work item or pending decision; not yet done.
- **DEFERRED-BY-DECISION** — John's explicit decision, with documented reason. *(In-code deliberate deferrals without recorded adjudication do NOT qualify — they stay OPEN.)*
- **SL-UNVERIFIED** — SL's own behavior not yet confirmed (live SL or captured traces needed); cannot be closed until it is.

**Current tally (updated 2026-07-21, post EXP-SLICE-7 `1e92caf85f`):** 39 VERIFIED-CONFORMANT · **0 OPEN** · 11 DEFERRED-BY-DECISION · **0 SL-UNVERIFIED** → **✅ COMPLETE / SL-COMPLIANT** by the ledger's own rule (every row is VERIFIED-CONFORMANT or DEFERRED-BY-DECISION). Slice 7 resolved the SL-UNVERIFIED tail (UNV-1/2/3/4/6/7, CAP-RE-ERR) as documented DEFERRED-BY-DECISION exceptions (defensible conservative behavior implemented; SL behavior undocumented & not empirically verifiable without Premium — D-SLTEST), and closed the last stragglers (DEC-4 trusted → VERIFIED; UNV-8 → N/A; UNV-9 → deferred). See the Compliance Summary below. (Slice 5 compliance fixes: consent timeout 120→300s [UNV-EXP-TIMEOUT], KV quota enforcement code 11 [DEC-2], llDataSizeKeyValue total-quota confirmed [UNV-5], OTH-1 agent-parcel. Deploy pending [Phlox.ScriptEngine.dll only]. Remaining code — Slice 6: acquire [DEC-3, the deliberate deviation], CAP-ICO cap serve, ExperienceQuery [CAP-EQ, needs D-EEP]. Then Slice 7: SL-UNVERIFIED live-test tail [UNV-1/2/3/4/6/7, CAP-RE-ERR].) (Slice 4 served CAP-GRP + CAP-GAE, broadened CAP-GCE to owner∪group, and made the association gate use owner∪GP_CREATOR — the three enumeration caps now match their three predicates [contributor/admin/group]. Legion serves **13 of 14 caps**; only ExperienceQuery [CAP-EQ] remains unserved. Deploy pending: Slice 3 LIVE per its deploy; Slice 4 committed, deploy pending. Remaining code — Slice 5 tail: consent timeout 120→300s, quota enforcement [DEC-2], acquire [DEC-3], CAP-ICO cap serve, ExperienceQuery [CAP-EQ, needs D-EEP]. Then Slice 7: SL-UNVERIFIED live-test tail.) (Slice 3 served CAP-ADM + CAP-UPD — the experience profile is now editable [admin-gated; group owner-only]. `group_id` is now writable, unblocking Slice 4 group integration. Legion serves 10 of 14 caps + association write. **Deploy pending**: Slices 2.1/2.2 now LIVE per the milestone deploy; Slice 3 committed, deploy pending. Remaining code: Slice 4 group — CAP-GRP/CAP-GAE + group-power union for GCE/contributor; Slice 5 tail — timeout 300s, quota, acquire, CAP-ICO cap, ExperienceQuery.) (Slice 2.1 fixed the script→experience association WRITE — CAP-ASSOC, the last gap before Experience is **usable end-to-end via the viewer**; Slice 2.2 named its permission gate as `IsExperienceContributor` (owner-only, no behavior change — there was never a stub; the owner case already worked). Legion serves 8 of 14 caps + the association write. Remaining: Slice 3 profile edit — CAP-ADM/CAP-UPD; Slice 4 group — CAP-GRP/CAP-GAE + CAP-GCE group-union + IsExperienceContributor group-power; Slice 5 tail — CAP-AGE-POST/CAP-ICO cap/CAP-EQ.)

---

## Section 1 — Script surface: fixed items (EXP-CONFORMANCE-2/3)

Evidence base for all nine: `experience-conformance-audit-v1.md`, commits `4ddbb8965d` (EXP-CONFORMANCE-2) and `2baa516eea` (EXP-CONFORMANCE-3). Deploy: per the EXP-CONFORMANCE-3 deploy note the deploy set is `Phlox.ScriptEngine.dll` + `OpenSim.Services.Interfaces` (comment-only rebuild). ⚠ *Live-grid deploy confirmation is cited from the commit's deploy note only; if a separate deploy log exists, link it here — otherwise these rows carry an implicit "pending deploy record" asterisk.*

| ID | Item | State | Evidence |
|----|------|-------|----------|
| SS-1 | `llGetExperienceDetails` returns SL layout `[name, owner key, experience id, state, state message, group key]` (was `[name, owner, description, group, maturity, ""]` — High, silent data corruption) | VERIFIED-CONFORMANT | audit §llGetExperienceDetails; FIX 1 in `4ddbb8965d` (LSLSystemAPI.cs:12732-12757) |
| SS-2 | `llCreateKeyValue` key-length cap 255 → `MAX_KEY_LENGTH` 1011 | VERIFIED-CONFORMANT | audit §llCreateKeyValue; FIX 2 in `4ddbb8965d` (LSLSystemAPI.cs:11363) |
| SS-3 | `llUpdateKeyValue` key-length cap 255 → 1011 | VERIFIED-CONFORMANT | audit §llUpdateKeyValue; FIX 2 in `4ddbb8965d` (LSLSystemAPI.cs:11424) |
| SS-4 | `llDeleteKeyValue` missing key emits 14 (KEY_NOT_FOUND), was 13 (STORAGE_EXCEPTION) | VERIFIED-CONFORMANT | audit §llDeleteKeyValue; FIX 3 in `4ddbb8965d` (LSLSystemAPI.cs:11453-11465) |
| SS-5 | `llGetExperienceErrorMessage` rows 14-16 corrected (were shifted/stale vs SL and Legion's own emit sites) | VERIFIED-CONFORMANT | FIX 3 companion in `4ddbb8965d` (LSLSystemAPI.cs:11966-11974) |
| SS-6 | `llAgentInExperience` requires root presence + reuses `HasExperiencePermission` (block-wins > admission > grant), was bare `IsAgentGranted` | VERIFIED-CONFORMANT | audit §llAgentInExperience; FIX 4 in `4ddbb8965d` (LSLSystemAPI.cs:12705-12726) |
| SS-7 | `llRequestExperiencePermissions` land-scope denials emit 17 (NOT_PERMITTED_LAND), agent-scope stays 4, no-experience stays 5 | VERIFIED-CONFORMANT | FIX 5 adjudicated (SL wiki error table Aug-2025) in `2baa516eea` (LSLSystemAPI.cs:12645-12659); supersedes EXP-ERRCODE-1/`5e1a8837f9` |
| SS-8 | `llGetExperienceErrorMessage` row 17 corrected to "not allowed to run on this land" | VERIFIED-CONFORMANT | `2baa516eea` (LSLSystemAPI.cs:11974-11976) |
| SS-9 | `llGetExperienceErrorMessage` row 18 added ("experience permissions request timed out"). **Update:** code 18 now HAS a real emit site as of DEC-1 (`9315d95324`) — the consent-dialog timeout. Was defined-unemitted under auto-grant; the message table entry was already correct | VERIFIED-CONFORMANT | `2baa516eea` (table) + `9315d95324` (emit site); see UNV-EXP-TIMEOUT |

## Section 2 — Viewer capabilities (14 SL caps)

Evidence base: `experience-caps-audit-v1.md` Part A (viewer wire formats read from secondlife/viewer `main`, 2026-07-19; Tranquillity cited as reference only).

### 2a. Served caps and their conformance sub-items

| ID | Item | State | Evidence / notes |
|----|------|-------|------------------|
| CAP-GEI | GetExperienceInfo — served; request parse + `experience_keys`/`error_ids` structure conformant | VERIFIED-CONFORMANT | ExperienceModule.cs:231-234, :396-425; viewer wire cross-check caps-audit A.1 |
| CAP-GEI-MAT | GetExperienceInfo `maturity` now sent as SIM_ACCESS 13/21/42 via `MaturityToSimAccess` (unknown → 42, over-restrict) | VERIFIED-CONFORMANT | commit `41ab709eb9` (FIX 0.1, decision D5); FS 7.2.2 indra_constants.h:163-167 + llfloaterexperienceprofile.cpp:267-288; deploy pending (John) |
| CAP-GEI-EXP | GetExperienceInfo rows now carry `expiration` = 600 (relative TTL; viewer converts to absolute) | VERIFIED-CONFORMANT | commit `41ab709eb9` (FIX 0.2a); FS llexperiencecache.cpp:69 (key), :230-232 (relative→absolute), :85 (600 = viewer DEFAULT_EXPIRATION); deploy pending (John) |
| CAP-GEI-META | GetExperienceInfo rows now carry `extended_metadata` — LLSD-XML string with `logo` + `marketplace`, always emitted (empty values hide their panels viewer-side) | VERIFIED-CONFORMANT | commit `41ab709eb9` (FIX 0.2b); FS llfloaterexperienceprofile.cpp:58,66,419-467 (LLSDXMLParser + key reads); deploy pending (John) |
| CAP-FBN | FindExperienceByName — served; query/page/page_size parse + `experience_keys` conformant | VERIFIED-CONFORMANT | ExperienceModule.cs:236-238, :428-451; caps-audit A.2 |
| CAP-FBN-PAGE | FindExperienceByName now pages in SQL (new `FindExperiences(query, offset, limit)`, stable ORDER BY; 50-row hard cap removed) and emits `next_page_url`/`previous_page_url` (page+1 probe row) | VERIFIED-CONFORMANT | commit `41ab709eb9` (FIX 0.3); FS llpanelexperiencepicker.cpp:248-249 (presence-only), :96-97,443-446 (re-request via `?page=N±1`), findExperienceByNameCoro URL shape; deploy pending (John) |
| CAP-RE | RegionExperiences — served; allowed/blocked GET/POST round-trip incl. estate authz conformant | VERIFIED-CONFORMANT | ExperienceModule.cs:221-223, :277-344; caps-audit A.3 |
| CAP-RE-TRUST | RegionExperiences `trusted` **list** (data/UI half) — schema `experience_trusted` (mirrors `experience_allowed`; idempotent additive bootstrap), service methods `GetTrustedExperiences`/`TrustExperience`/`RemoveTrustedExperience` (blocked-exclusive, allowed-compatible; `BlockExperience` now clears trusted too), GET returns real `trusted` array, POST reconciles it. Wire key verified `content["trusted"]` | VERIFIED-CONFORMANT | commit `2238a1b0e2` (EXP-SLICE-0.5, DEC-4/Option A); FS 7.2.2 llfloaterregioninfo.cpp:3274 (GET read), :3428 (POST write), :3426-3428 (independent lists); deploy pending (John) |
| CAP-RE-TRUST-ENF | Trusted **enforcement**: a region-trusted experience now (a) bypasses the consent dialog — granted silently in `llRequestExperiencePermissions` before any prompt — and (b) satisfies admission in `IsExperienceAdmitted`. Both seams wired | VERIFIED-CONFORMANT | commit `9315d95324` (landed with DEC-1); trusted∩blocked kept empty at write time + block-wins applied before admission, so a blocked experience is never re-admitted; deploy pending (John) |
| CAP-RE-DEF | RegionExperiences `default` omission **verified correct**: the Region panel reads the key conditionally (`if(content.has("default"))`), so omission is the proper wire encoding of "no default experience" — a concept Legion doesn't model. No wire change; builder comment now cites the evidence | VERIFIED-CONFORMANT | commit `41ab709eb9` (FIX 0.4a); FS llfloaterregioninfo.cpp:3266-3268 |
| CAP-RE-ERR | RegionExperiences error case. **Known:** SL's exact error-response shape is undocumented; the Firestorm panel re-reads the lists on response. **Chosen:** denial/parse-error echoes the CURRENT lists (reject-shaped) — the panel visibly reverts, never breaks. Viewer-safe; behavior already correct, no code change. **Verify vs live SL** | DEFERRED-BY-DECISION | commit `1e92caf85f` (D-SLTEST); caps-audit A.3 |

### 2b. Missing caps (11)

| ID | Item | State | Readiness (caps-audit ref) |
|----|------|-------|---------------------------|
| CAP-GEX | GetExperiences — served. GET → `{experiences:[granted], blocked:[blocked]}` → Allowed/Blocked tabs. Added the missing service method `GetAgentBlockedExperiences` (granted=0) — no schema change (agent-block already existed via `DenyPermission`) | VERIFIED-CONFORMANT | commit `a83d3f49f8` (Slice 2); FS 7.2.2 llfloaterexperiences.cpp:146-147,395; deploy pending (John) |
| CAP-AGE | AgentExperiences GET — served. `{experience_ids:[owned]}` via `GetExperiencesByOwner` → Owned tab; POST returns the owned list without acquiring (Acquire button stays disabled — `purchase` key omitted) | VERIFIED-CONFORMANT | commit `a83d3f49f8` (Slice 2); FS 7.2.2 llfloaterexperiences.cpp:148,155; deploy pending (John) |
| CAP-AGE-POST | AgentExperiences POST — "Acquire an Experience" served. **DEC-3 deliberate deviation**: SL gates on Premium; Legion uses grid-config `[Experience] ExperienceCreators` (default `EstateManagersAndRegionOwners` via `CanIssueEstateCommand`; also `Anyone`/`AdminsOnly`). Permitted → `CreateExperience(owner=agent, empty name→fresh UUID)`; response `{experience_ids, purchase:0}` — the `purchase` key enables the Acquire button only for permitted agents; viewer diffs and opens the new experience's profile | VERIFIED-CONFORMANT (with documented DEC-3 deviation) | commit `33bd4fe995` (Slice 6); FS 7.2.2 llfloaterexperiences.cpp:240,246-264 |
| CAP-GCE | GetCreatorExperiences — served. GET bare URL → `{experience_ids:[…]}` from `GetExperiencesByOwner(agentID)`; registered in seed caps. Un-greys the script editor Experience dropdown + fills the Contributor tab. **Owner-only core**; SL's group-power (ExperienceCreator) union still pending the CAP-GRP `GetExperiencesByGroup` query (Slice 4) — noted, not stubbed | VERIFIED-CONFORMANT | commit `a12d414f79` (Slice 1); FS 7.2.2 llpreviewscript.cpp:2039-2057, llfloaterexperiences.cpp:153,251; deploy pending (John) |
| CAP-GAE | GetAdminExperiences — served. GET bare URL → `{experience_ids}` = owner ∪ experiences whose group the agent holds **GP_EXPERIENCE_ADMIN** in → Admin tab. Consistent with `IsAgentExperienceAdmin` | VERIFIED-CONFORMANT | commit `e96b1a14cd` (Slice 4); FS 7.2.2 llfloaterexperiences.cpp `updateInfo("GetAdminExperiences","Admin_Experiences_Tab")` |
| CAP-EPREF | ExperiencePreferences — served, on the John-approved **dedicated `experience_agent_blocked` table** (8th table; mirrors `experience_trusted`). GET `?<exp_id>` reads; PUT `{"<exp_id>":{permission:"Allow"\|"Block"}}` → Allow=Unblock+Grant / Block=Block+Forget (mutually exclusive); DELETE `?<exp_id>` → Forget+Unblock (undecided); all return `{experiences, blocked}`. **Closes the D1 consent Block loop** — Block button PUT → `BlockExperienceForAgent` → persisted → next `llRequestExperiencePermissions` denies at the early gate (`IsAgentBlocked`, now reading the new table, moved **before** the already-granted check so block wins even over a stale grant; code 4) | VERIFIED-CONFORMANT | commits `a83d3f49f8` + `5bdfadcd4f` (Slice 2 CAP3); FS 7.2.2 llexperiencecache.cpp:762-839, llfloaterexperienceprofile.cpp:487-488,892-894; deploy pending (John) |
| CAP-META | GetMetadata — served. POST `{object-id,item-id,fields:["experience"]}` → `{experience:<uuid>}` via `GetScriptExperience(itemId)` (in-memory map + persisted `script_experiences`); no association → key omitted (viewer treats as benign "no experience"); registered in seed caps | VERIFIED-CONFORMANT | commit `a12d414f79` (Slice 1); FS 7.2.2 llexperiencecache.cpp:596-603 (send), :608/:626 (read). `fields` beyond `experience`: viewer never requests them (UNV-8) |
| CAP-ASSOC | Script→experience **association WRITE** — the `UpdateScriptTask` cap now honors the `experience` field in the save body (not a separate cap). Zero → clear (uncheck); non-zero → associate if the agent is a contributor via `IExperienceService.IsExperienceContributor` (owner-only, matches owner-only CAP-GCE; group union deferred to Slice 4). Routed through `IExperienceModule.TryAssociateScriptExperience` so the module cache stays coherent (raw-service write would leave a stale cached Zero). **This was the gap causing code 5 despite the un-greyed UI** — completes list → un-grey → associate → consent | VERIFIED-CONFORMANT | commits `b506141780` (Slice 2.1) + `b900a84218` (Slice 2.2, named contributor check); FS 7.2.2 llviewerassetupload.cpp:873 (field `experience`, task branch only), llpreviewscript.cpp:3244/3254-3256; deploy pending (John) |
| CAP-GRP | GroupExperiences — served. GET `?<group_id>` → `{experience_ids}` of that group via new service `GetExperiencesByGroup` (mirrors by-owner). Now populated because CAP-UPD (Slice 3) writes `group_id` | VERIFIED-CONFORMANT | commit `e96b1a14cd` (Slice 4); FS 7.2.2 llpanelgroupexperiences.cpp:76, llexperiencecache getGroupExperiences (`?`+groupId, reads `experience_ids`) |
| CAP-GCE-GRP | GetCreatorExperiences **group union** (Slice-1 owner-core was VERIFIED) — now owner ∪ experiences whose group the agent holds GP_EXPERIENCE_CREATOR in; the script-association gate uses the matching module `IsAgentExperienceContributor`, so the dropdown lists exactly what the gate accepts | VERIFIED-CONFORMANT | commit `e96b1a14cd` (Slice 4) |
| CAP-ADM | IsExperienceAdmin — served. GET `?experience_id=` → `{status:bool}`, gates the profile Edit button. Full check = owner OR group **GP_EXPERIENCE_ADMIN** (bit 49) via `IGroupsModule.GetMembershipData`; service `IsExperienceAdmin` is owner-only (parallel to `IsExperienceContributor`), module ORs the group-power | VERIFIED-CONFORMANT | commit `75ff65338c` (Slice 3); FS 7.2.2 llexperiencecache.cpp getExperienceAdmin (`?experience_id=`, reads `status`); GroupsService.cs:98 |
| CAP-UPD | UpdateExperience — served. POST the experience map → `{experience_keys:[updated]}` (reuses `ExperienceToOSD`, maturity round-trips 13/21/42 via `SimAccessToMaturity`↔`MaturityToSimAccess`; props reversed via `ViewerPropsToInternal`). **Admin-gated** (CAP-ADM); **`group_id` OWNER-ONLY** (non-owner admin keeps existing group). Writes via service `UpdateExperience`. **`group_id` now writable → unblocks Slice 4 group integration** | VERIFIED-CONFORMANT | commit `75ff65338c` (Slice 3); FS 7.2.2 llfloaterexperienceprofile.cpp updatePackage:786-836, llexperiencecache.cpp updateExperienceCoro postAndSuspend:904-921 |
| CAP-EQ | ExperienceQuery — served as a **documented no-op** (D-EEP resolved). SL uses it to clear experience-driven agent-environment injections no longer permitted on the agent's parcel; **Legion's per-agent EEP injection is stubbed** (`llSetAgentEnvironment`/`llReplaceAgentEnvironment` do nothing, LSLSystemAPI.cs:11974,11995), so there is nothing to police. Serves `{experiences:{id:true}}` (clears nothing). Revisit if per-agent EEP injection is implemented | DEFERRED-BY-DECISION | commit `33bd4fe995` (Slice 6, D-EEP); FS 7.2.2 llenvironment.cpp:2182,2705 |
| CAP-ICO | IsExperienceContributor — **cap now served.** GET `?experience_id=<uuid>` → `{status:bool}` via the owner∪GP_CREATOR predicate (`IsAgentExperienceContributor`). Parity-only (current Firestorm never calls it — contributor inferred from GetCreatorExperiences); shape per Tranquillity (UNV-9) | VERIFIED-CONFORMANT | commit `33bd4fe995` (Slice 6); shape mirrors CAP-ADM |

## Section 3 — Policy decisions pending (all currently OPEN — none has a recorded decision)

| ID | Item | State | Notes |
|----|------|-------|-------|
| DEC-1 | **Consent model**: real SL consent implemented — auto-grant replaced by prompt/await. `llRequestExperiencePermissions` now sends `ScriptQuestionExperience` (via the experience-aware `SendScriptQuestion` overload + `JoinAnExperience` bit 0x2000) and awaits `ScriptAnswerYes`: accepted → grant + `experience_permissions`; denied → `_denied` code 4; timeout → code 18; disconnect → denied+cleanup. Trusted experiences bypass the dialog (grant silently). Gate confirmed OpenMetaverse 0.9.4 supports the packet path | VERIFIED-CONFORMANT | commit `9315d95324` (DEC-1); FS 7.2.2 llviewermessage.cpp:7297/7341/7459, llscriptruntimeperms.h:63; deploy pending (John). Resolves the SS-9 code-18 gap. Note CAP-EPREF (resident opt-out via profile) still OPEN — the dialog's Block button needs it |
| DEC-2 | **128 MiB KV quota — now ENFORCED.** `llCreateKeyValue`/`llUpdateKeyValue` check projected size before write and emit **XP_ERROR_QUOTA_EXCEEDED (11)** if it would exceed `MAX_DATA_QUOTA` (128 MiB), no write. Basis: key+value UTF-8 bytes (matches `DataSizeKeyValue`'s `SUM(LENGTH(kv_key)+LENGTH(kv_value))`; SL's exact basis undocumented → conservative keys+values, documented). Tracking: SUM-at-write (no schema change / no counter drift). Update path is delta-aware (reads old value) | VERIFIED-CONFORMANT | commit `385a6d891d` (Slice 5); SL wiki llCreateKeyValue/llUpdateKeyValue (128 MiB, code 11) |
| DEC-3 | AgentExperiences POST acquire policy — **DECIDED + implemented** (= CAP-AGE-POST): grid-config `[Experience] ExperienceCreators`, default estate-managers + region-owners (SL's Premium gate is the deliberate deviation) | DEFERRED-BY-DECISION | commit `33bd4fe995` (Slice 6) |
| DEC-4 | Trusted experiences — **decided + implemented** (the "ratify or schedule" decision resolved to *build it*): trusted list + enforcement shipped and verified (see CAP-RE-TRUST + CAP-RE-TRUST-ENF). Matches SL's trusted/key model, not a deviation | VERIFIED-CONFORMANT | commits `2238a1b0e2` + `9315d95324`; caps-audit D4 |
| D-NAME | UpdateExperience Name/Description **content filter**: SL runs them through a G-rating check with an **unpublished objectionable-word list** — not replicable exactly. Per decision, NO guessed filter is implemented (a wrong filter is a half-measure the other way). Only verifiable validation is enforced: required non-empty name + length clamps (name 64, desc 256) | DEFERRED-BY-DECISION | commit `75ff65338c` (Slice 3); documented in-code at `HandleUpdateExperience`. The single acquire-policy deviation (DEC-3) plus this are the intended exceptions |

## Section 4 — SL-behavior-unverified items (from experience-conformance-audit-v1 unless noted)

| ID | Item | State | Notes |
|----|------|-------|-------|
| UNV-1 | Empty-value read. **Known:** SL wiki documents a *missing* key → KEY_NOT_FOUND (14); it does NOT document what an EXISTING key with an empty-string value returns. **Chosen:** existing-empty → `1,` (success, empty value — data-preserving; an existing key is not "not found"); absent → `0,14`. **Verify vs live SL if Premium access appears** | DEFERRED-BY-DECISION | commit `1e92caf85f` (D-SLTEST); SL wiki llReadKeyValue |
| UNV-2 | CAS vs empty original. **Known:** SL CAS updates only if current == original (mismatch → 15); the empty-original case is undocumented. **Chosen:** empty original is a *real comparand* — update only if the stored value is currently empty (new `UpdateKeyValue(…,conditional)` overload), never a wildcard. **Verify vs live SL** | DEFERRED-BY-DECISION | commit `1e92caf85f` (D-SLTEST); SL wiki llUpdateKeyValue |
| UNV-3 | CAS-fail vs key-missing distinction. **Known:** SL uses 15 (RETRY_UPDATE) for a checked-update mismatch; whether a missing key is 14 vs 15 is undocumented. **Chosen:** the service returns bare false for CAS-fail/missing/error alike → emit 15 for all (SL-valid CAS-fail code). **Verify vs live SL** | DEFERRED-BY-DECISION | commit `1e92caf85f` (D-SLTEST) |
| UNV-4 | `llKeysKeyValue` clamps. **Known:** SL paginates; exact default/clamp values undocumented. **Chosen:** count≤0 → 100 (sane default page), cap 1000 (matches the service `KeysKeyValue` clamp), start floored at 0. **Verify vs live SL** | DEFERRED-BY-DECISION | commit `1e92caf85f` (D-SLTEST) |
| UNV-5 | `llDataSizeKeyValue` second value = **TOTAL quota** (128 MiB), not free-remaining — confirmed from SL wiki (llDataSizeKeyValue); Legion already emits `1,<used>,<128MiB>` correctly | VERIFIED-CONFORMANT | commit `385a6d891d` (comment corrected); SL wiki llDataSizeKeyValue |
| UNV-6 | Root-presence requirement. **Known:** SL's exact presence requirement for the target agent is undocumented. **Chosen:** deny (code 4) if the agent is absent or a child agent — conservative, can never over-grant a not-truly-present agent. **Verify vs live SL** | DEFERRED-BY-DECISION | commit `1e92caf85f` (D-SLTEST) |
| UNV-7 | Ladder tie-breaks. **Known:** SL's block-wins land model is documented; the fine-grain tie-break order among rules is not. **Chosen:** most-restrictive-wins, fixed order block → admission → agent-block → grant (a deny at any tier wins). **Verify vs live SL** | DEFERRED-BY-DECISION | commit `1e92caf85f` (D-SLTEST) |
| UNV-8 | GetMetadata `fields` beyond `experience` — **closed as N/A.** The viewer only ever requests `fields:["experience"]` (llexperiencecache.cpp:596-601), and Legion serves exactly that; SL's broader field vocabulary is irrelevant to any real client. No gap | VERIFIED-CONFORMANT | closed-by-viewer-behavior; caps-audit A.9 |
| UNV-9 | IsExperienceContributor cap wire shape — the cap is now served (Slice 6) with the Tranquillity-based shape `?experience_id=`→`{status:bool}`; the current Firestorm never calls it, so SL's exact server-side shape is academic. **Chosen:** Tranquillity shape (mirrors IsExperienceAdmin). Verify vs live SL/a TPV that calls it, if ever | DEFERRED-BY-DECISION | commit `33bd4fe995` (Slice 6); caps-audit A.14 |
| UNV-EXP-TIMEOUT | Consent dialog timeout now **300s** — SL wiki (llRequestExperiencePermissions) documents "times out after **at least 5 minutes**" (300s floor); D1's 120s was non-compliant, now fixed. code-18 emit path unchanged | VERIFIED-CONFORMANT | commit `385a6d891d` (Slice 5, `EXPERIENCE_PERM_TIMEOUT_MS=300000`); SL wiki llRequestExperiencePermissions |

## Section 5 — Other open items

| ID | Item | State | Notes |
|----|------|-------|-------|
| OTH-1 | **Fixed** — `HasExperiencePermission` (the agent-affecting gate) now evaluates parcel block + admission at the **AGENT's** parcel when the agent has a root presence in this region (falls back to the object's parcel otherwise). Parcel checks parameterized by position (`…OnParcelAt`/`IsExperienceAdmittedAt`); object-context callers (`llRequestExperiencePermissions` grant-time) keep the object's parcel | VERIFIED-CONFORMANT | commit `385a6d891d` (Slice 5); experience-conformance-audit-v1 §Permission/admission model |

## Observations (not parity rows)

- **OBS-1:** `ExperienceService` is per-region direct-MySQL (ExperienceModule.cs:103), no Robust connector; grid-consistency relies on a shared `[Experience]` connection string. Architecture note — becomes a row only if it produces observable divergence.
- **OBS-2:** `llClearKeyValue` (10,000-keys-per-call enumerate-then-delete) is a Legion extension outside SL's surface — per the prior audit's carve-out, not a parity item.
- **OBS-3:** Recon/design history lives in `Build Documents/` (`EXPERIENCE_WIRING_RECON.md`, `EXP_PERSIST_RECON.md`, `EXP_KV_SLCONFORMANCE.md`, `EXP_KV_ASYNC_RECON.md`, `EXP_KV_PAYLOAD_SPEC.md`) and `tranquillity-port-audit-v1.md` Phase 2 §C.

---

## Addendum 2026-07-19 — D5/D1 verified against local Firestorm source (read-only recon)

**Source verified:** Firestorm **7.2.2** — git checkout at `D:\AI Tools\Firestorm Viewer working Code\phoenix-firestorm` (HEAD `f36da6af09`; a second unpacked 7.2.2 copy exists at `D:\AI Tools\Firstorm Viewer Source Code`). This is the viewer Legion's users actually run; 7.2.2 is a current-generation FS release, and its experience code is structurally identical to secondlife/viewer `main` (checked 2026-07-19), so version drift risk is low.

### D5 — maturity constants: **13 / 21 / 42 CONFIRMED**
- `indra/llcommon/indra_constants.h:163-167`: `SIM_ACCESS_MIN = 0` ("treated as unknown, usually ends up PG"), `SIM_ACCESS_PG = 13`, `SIM_ACCESS_MATURE = 21`, `SIM_ACCESS_ADULT = 42`.
- Consumption: `llfloaterexperienceprofile.cpp:267-288` `setMaturityString(U8 maturity, ...)` — the value read from the GetExperienceInfo response's `maturity` key is compared with `<= SIM_ACCESS_PG` → General, `<= SIM_ACCESS_MATURE` → Moderate, `<= SIM_ACCESS_ADULT` → Adult.
- **Consequence for Legion today:** sending 0/1/2 means every experience — including Adult ones — displays as **General** in the viewer (0, 1, 2 are all ≤ 13). Rating is misrepresented, not just missing. (Same comparison style presumably governs the search picker's rating gating — not separately verified.)
- **Verdict:** GetExperienceInfo (and UpdateExperience when implemented) must send/accept `maturity` ∈ {13, 21, 42}. The audit's 13/21/42 claim is **confirmed from the shipped viewer's source**, no longer inferred from Tranquillity. CAP-GEI-MAT fix is unblocked.

### D1 — consent dialog: **fully supported by Firestorm 7.2.2; Legion never triggers it**
The viewer has complete, working consent-dialog code (`indra/newview/llviewermessage.cpp`):
1. **Trigger — sim-side contract:** the standard **ScriptQuestion** UDP message with the optional **`Experience` block** appended:
   - `Data` block: `TaskID` (object), `ItemID` (script item), `ObjectName`, `ObjectOwner`, `Questions` (S32 permission bitmask);
   - `Experience` block: `ExperienceID` (UUID) — detected at `process_script_question` (llviewermessage.cpp:7297-7300: `msg->has(_PREHASH_Experience)`).
2. When the Experience block is present, the viewer resolves the experience via **GetExperienceInfo** (which Legion already serves — llviewermessage.cpp:7462) and shows notification **`ScriptQuestionExperience`** (notifications.xml:9188: "…requests your participation in the [GRID_WIDE] experience… Once permission is granted you will not see this message again…"), with Yes / No / **Mute** / **Block Experience** buttons and grid-vs-land scope derived from the `properties` PROPERTY_GRID bit (:7262).
3. **Answer path:** the viewer replies with **ScriptAnswerYes** (`TaskID`, `ItemID`, `Questions` = granted bitmask; 0 on deny — llviewermessage.cpp:7185-7191). The sim must interpret an experience-tagged answer as persist-grant (→ `experience_permissions` event) or deny (→ `experience_permissions_denied`; exact SL denial code for user-decline not yet verified — likely 4). **The "Block Experience" button PUTs `Block` via the ExperiencePreferences cap** (:7214-7222) — i.e. CAP-EPREF is a hard dependency of a fully-functional consent dialog.
4. **Legion gap confirmed concretely:** `llRequestExperiencePermissions` (LSLSystemAPI.cs:12622-12710) runs block/admission/presence checks then **auto-grants at :12695-12705 without ever contacting the viewer**. Legion *cannot* currently send the trigger even if it wanted to: `IClientAPI.SendScriptQuestion` (IClientAPI.cs:1259) and `LLClientView.SendScriptQuestion` (LLClientView.cs:12278-12287) have **no experience parameter** and build the packet without an Experience block. Implementing SL consent needs: signature extension + Experience block emission + ScriptAnswerYes handling that routes experience answers to `GrantPermission`/`DenyPermission` (+ timeout → code 18, currently unemitted per SS-9).
5. Packet-library support: Legion's binary `bin/OpenMetaverse.dll` (v0.9.4) metadata contains `ScriptQuestionPacket`, `ExperienceBlock`, `ExperienceID` — the block almost certainly exists in the packet class already. *(Metadata-string evidence only — confirm by reflection on .NET 8 or source lookup before relying on it.)*

**Verdict for DEC-1:** the auto-grant's stated premise ("viewer-native experience dialogs require viewer support the grid can't rely on") is **false for Firestorm 7.2.2** — the dialog ships, wired end-to-end, waiting on a sim that sends the Experience block. Real SL consent is implementable. The decision remains John's (auto-grant may still be preferred as UX policy), but it is now a pure policy choice, not a technical constraint. Recommended sequencing if consent is adopted: CAP-EPREF first or simultaneously (Block button), then the ScriptQuestion/Experience plumbing.
